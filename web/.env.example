VITE_API_URL=http://localhost:3000

# Google OAuth 2.0 Client ID (Google Cloud Console). Must match the
# backend's GOOGLE_CLIENT_ID — used to obtain a Google ID token that
# gets verified server-side by POST /auth/google.
VITE_GOOGLE_CLIENT_ID=
